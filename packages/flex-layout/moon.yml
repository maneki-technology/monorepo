$schema: 'https://moonrepo.dev/schemas/project.json'

language: 'typescript'

dependsOn:
  - 'foundation'

tasks:
  build:
    script: 'vite build && tsc --emitDeclarationOnly'
    inputs:
      - 'src/**/*'
      - 'tsconfig.json'
      - 'vite.config.ts'
    outputs:
      - 'dist'
    deps:
      - '^:build'

  test:
    command: 'vitest --run'
    inputs:
      - 'src/**/*'
      - 'tsconfig.json'
      - 'vite.config.ts'

  test-watch:
    command: 'vitest'
    inputs:
      - 'src/**/*'
    preset: 'server'

  dev:
    command: 'vite'
    preset: 'server'
